# Messenger mobile behavior

The floating button sits above WorkflowOS's bottom navigation. The composer width is constrained to the viewport with a scrollable body so subject, message and send controls remain usable on smaller devices.
