# Owner messenger behavior

Opening the floating action keeps the owner on the current WorkflowOS page. Private mode requires an active staff recipient. Team mode sends to all active non-owner profiles returned by the existing organization-scoped query. The composer clears after a successful send and displays the server result.
