# Deployment note

Do not merge this branch solely because the UI compiles. The exact branch head must have successful repository verification and a READY Vercel preview, then be rechecked against current main immediately before squash merge.
