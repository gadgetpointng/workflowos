# Messenger error handling

The composer displays server-provided authorization, validation and delivery failures. Send controls are disabled while sending and when required fields are empty. A failed response does not clear the draft, allowing the owner to correct the issue and retry.
