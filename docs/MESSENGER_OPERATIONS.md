# Messenger operations

Delivery failures are surfaced to the owner composer from the API response. Operators should not manually duplicate a message after an ambiguous network failure until checking notifications/activity history, to avoid duplicate staff messages.

Owner Control remains the source of truth for whether private messaging and team feed are enabled.
