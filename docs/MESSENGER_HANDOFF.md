# Handoff

Once deployed, the next engineering task should be a true conversation model only if the current owner-to-staff delivery proves useful in production. Avoid mixing that schema migration into this compatibility release.
