# Rollback

If the floating surface causes a production regression, remove the `FloatingMessenger` mount from `WorkspaceShell`; the established communications pages and server delivery route continue to exist.
