# Production verification

After merge, require the Vercel production deployment for the resulting merge commit to reach READY and confirm the WorkflowOS health endpoint remains HTTP 200 with `ok: true`.
