# Verification checks

The focused workflow lints the messenger component, shared shell, and owner communications route, then runs the production build. Existing repository checks remain authoritative for broader boundaries.
