# PWA release gate

This release candidate must pass an exact-head Vercel preview before merge. The service worker must continue to exclude API, authentication, and authenticated workspace data from caching.
