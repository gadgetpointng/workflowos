# Messenger compatibility

The floating messenger is mounted in the shared WorkspaceShell, so it follows the deployed Admin-style desktop collapse behavior and remains available across WorkflowOS routes. On mobile it is positioned above the existing five-item bottom navigation.
