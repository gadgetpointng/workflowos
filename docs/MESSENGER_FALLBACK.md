# Fallback behavior

The existing `/owner-communications` and `/inbox` routes remain untouched as full-page fallbacks. This allows the floating messenger to be rolled back independently without losing communications access.
