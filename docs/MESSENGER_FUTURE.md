# Messenger next phase

After this compatible first release is stable, the next phase can introduce true two-way threaded conversations with a dedicated conversation/message schema, unread counters, live updates, attachments and richer Admin parity. That phase should migrate existing owner notifications into threads rather than replacing working delivery abruptly.
