'use client';
import {FormEvent,useState} from 'react';
export default function GoalQuickCreate(){const [busy,setBusy]=useState(false);const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg('');const f=new FormData(e.currentTarget);const r=await fetch('/api/goals',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});setBusy(false);if(r.ok){e.currentTarget.reset();location.reload()}else setMsg((await r.json()).error||'Could not create goal')}return <form onSubmit={submit} className="rounded-3xl border bg-white p-5 shadow-sm"><h2 className="text-lg font-bold">New growth goal</h2><p className="mt-1 text-sm text-slate-500">Give the team a measurable outcome.</p><div className="mt-4 grid gap-3"><input name="name" required placeholder="e.g. ₦5m online revenue" className="rounded-xl border px-3 py-2"/><input name="metric" required placeholder="Metric: revenue, leads, orders..." className="rounded-xl border px-3 py-2"/><div className="grid grid-cols-2 gap-3"><input name="target_value" type="number" step="0.01" required placeholder="Target" className="rounded-xl border px-3 py-2"/><input name="ends_at" type="date" className="rounded-xl border px-3 py-2"/></div><textarea name="description" placeholder="What does success look like?" className="min-h-24 rounded-xl border px-3 py-2"/><button disabled={busy} className="rounded-xl bg-slate-950 px-4 py-2 font-semibold text-white disabled:opacity-50">{busy?'Creating…':'Create goal'}</button>{msg&&<div className="text-sm text-rose-600">{msg}</div>}</div></form>}
