'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export function SLAScan(){const [b,setB]=useState(false);const r=useRouter();return <button className="primary-button" disabled={b} onClick={async()=>{setB(true);await fetch('/api/sla',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'scan'})});setB(false);r.refresh()}}>{b?'Scanning…':'Scan now'}</button>}export function SLAResolve({id}:{id:string}){const r=useRouter();return <button className="topbar-pill" onClick={async()=>{await fetch('/api/sla',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({action:'resolve',id})});r.refresh()}}>Resolve</button>}
