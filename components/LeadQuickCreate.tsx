'use client';
import { FormEvent, useState } from 'react';
import { useRouter } from 'next/navigation';
export default function LeadQuickCreate(){const router=useRouter();const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const fd=new FormData(e.currentTarget);const payload={name:fd.get('name'),phone:fd.get('phone'),source:fd.get('source'),product_interest:fd.get('product_interest')};const res=await fetch('/api/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const body=await res.json();setBusy(false);if(!res.ok){setError(body.error||'Could not add lead');return;}e.currentTarget.reset();router.refresh();}return <form onSubmit={submit} className="quick-create-grid lead-grid"><input name="name" placeholder="Customer name" className="app-input"/><input name="phone" placeholder="Phone / WhatsApp" className="app-input"/><select name="source" defaultValue="manual" className="app-input"><option value="manual">Manual</option><option value="whatsapp">WhatsApp</option><option value="storefront">Storefront</option><option value="facebook">Facebook / Instagram</option><option value="facebook_marketplace">Facebook Marketplace</option><option value="tiktok">TikTok</option><option value="jumia">Jumia</option><option value="jiji">Jiji</option><option value="konga">Konga</option></select><input name="product_interest" placeholder="Product interest" className="app-input"/><button disabled={busy} className="primary-button">{busy?'Adding…':'Add lead'}</button>{error&&<div className="form-error">{error}</div>}</form>}
