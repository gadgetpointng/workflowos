import {NextResponse} from 'next/server';import {requireUser,canManage} from '@/lib/auth';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,profile}=await requireUser();if(!profile)return NextResponse.json({error:'Unauthorized'},{status:401});if(!canManage(profile.role))return NextResponse.json({error:'Manager access required'},{status:403});const b=await req.json();const patch:any={};for(const k of ['name','description','metric','target_value','current_value','ends_at'])if(k in b)patch[k]=b[k];const {data,error}=await supabase.from('goals').update(patch).eq('id',id).eq('organization_id',profile.organization_id).select().single();return NextResponse.json(error?{error:error.message}:{data},{status:error?400:200})}
