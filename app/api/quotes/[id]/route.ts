import { NextResponse } from 'next/server';
import { requireUser } from '@/lib/auth';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,user,profile}=await requireUser();if(!user||!profile)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();const patch:any={updated_at:new Date().toISOString()};for(const k of ['status','valid_until','notes','discount_amount','total_amount'])if(b[k]!==undefined)patch[k]=b[k];if(b.status==='sent')patch.sent_at=new Date().toISOString();if(b.status==='accepted')patch.accepted_at=new Date().toISOString();const {data,error}=await supabase.from('quotes').update(patch).eq('id',id).eq('organization_id',profile.organization_id).select().single();if(error)return NextResponse.json({error:error.message},{status:400});if(b.status==='accepted'&&data.deal_id)await supabase.from('deals').update({stage:'won',won_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq('id',data.deal_id);await supabase.from('activity_logs').insert({organization_id:profile.organization_id,actor_id:user.id,action:'quote.updated',entity_type:'quote',entity_id:id,metadata:{status:b.status||data.status}});return NextResponse.json({data});}
