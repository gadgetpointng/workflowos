import { NextResponse } from 'next/server'; import { requireUser,canManage } from '@/lib/auth';
const allowed=['accepted','in_progress','submitted','approved','completed','rejected','cancelled','assigned'];
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const {supabase,user,profile}=await requireUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const {data:task}=await supabase.from('tasks').select('*').eq('id',id).single();if(!task)return NextResponse.json({error:'Not found'},{status:404});const b=await req.json();if(b.status&&!allowed.includes(b.status))return NextResponse.json({error:'Invalid status'},{status:400});const manager=canManage(profile?.role);if(!manager&&task.assignee_id!==user.id)return NextResponse.json({error:'Forbidden'},{status:403});if(['approved','rejected','cancelled'].includes(b.status)&&!manager)return NextResponse.json({error:'Manager approval required'},{status:403});const patch:any={updated_at:new Date().toISOString()};for(const k of ['status','completion_notes','completion_evidence_url','approval_notes','assignee_id','due_at','priority'])if(b[k]!==undefined)patch[k]=b[k];const {data,error}=await supabase.from('tasks').update(patch).eq('id',id).select().single();if(error)return NextResponse.json({error:error.message},{status:400});await supabase.from('activity_logs').insert({organization_id:profile.organization_id,actor_id:user.id,action:`task.${b.status||'updated'}`,entity_type:'task',entity_id:id,metadata:{from:task.status,to:b.status}});if(task.creator_id&&b.status==='submitted')await supabase.from('notifications').insert({organization_id:profile.organization_id,recipient_id:task.creator_id,title:'Task submitted for approval',body:task.title,type:'task_submitted'});return NextResponse.json({data});}
