'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function AvailabilityEditor({userId,current='available',canEdit=true}:{userId:string;current?:string;canEdit?:boolean}){const [status,setStatus]=useState(current);const [busy,setBusy]=useState(false);const r=useRouter();async function save(){setBusy(true);await fetch('/api/availability',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({user_id:userId,status})});setBusy(false);r.refresh()}return <div className="flex gap-2"><select className="app-input" value={status} disabled={!canEdit} onChange={e=>setStatus(e.target.value)}><option>available</option><option>busy</option><option>away</option><option>offline</option><option>leave</option></select>{canEdit&&<button className="primary-button" disabled={busy} onClick={save}>{busy?'Saving…':'Save'}</button>}</div>}
