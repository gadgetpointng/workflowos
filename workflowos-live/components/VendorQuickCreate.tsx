'use client';
import { useState } from 'react'; import { useRouter } from 'next/navigation';
export default function VendorQuickCreate(){const r=useRouter();const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function submit(fd:FormData){setBusy(true);setError('');const res=await fetch('/api/vendors',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:fd.get('name'),contact_email:fd.get('email'),contact_phone:fd.get('phone'),source_url:fd.get('url'),commission_rate:Number(fd.get('commission')||0)})});if(!res.ok){setError((await res.json()).error||'Could not add vendor');setBusy(false);return;}r.refresh();setBusy(false);}return <form action={submit} className="rounded-3xl border bg-white p-5 shadow-sm"><div className="font-semibold">Add supplier / vendor</div><div className="mt-4 grid gap-3"><input name="name" required placeholder="Vendor name" className="rounded-xl border px-3 py-2"/><input name="email" type="email" placeholder="Email" className="rounded-xl border px-3 py-2"/><input name="phone" placeholder="Phone" className="rounded-xl border px-3 py-2"/><input name="url" placeholder="Original website URL" className="rounded-xl border px-3 py-2"/><label className="text-xs font-semibold text-slate-500">Your commission %</label><input name="commission" type="number" min="0" max="100" step="0.1" defaultValue="10" className="rounded-xl border px-3 py-2"/><button disabled={busy} className="rounded-xl bg-slate-900 px-4 py-2 font-semibold text-white">{busy?'Adding…':'Add vendor'}</button>{error&&<div className="text-sm text-red-600">{error}</div>}</div></form>}
