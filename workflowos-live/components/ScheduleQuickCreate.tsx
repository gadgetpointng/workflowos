'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';export default function ScheduleQuickCreate(){const [title,setTitle]=useState('');const [start,setStart]=useState('');const [kind,setKind]=useState('work');const [busy,setBusy]=useState(false);const r=useRouter();async function add(){if(!title||!start)return;setBusy(true);await fetch('/api/schedule',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({title,starts_at:new Date(start).toISOString(),event_type:kind})});setTitle('');setStart('');setBusy(false);r.refresh()}return <div className="quick-create-grid"><input className="app-input" placeholder="Event or deadline" value={title} onChange={e=>setTitle(e.target.value)}/><input className="app-input" type="datetime-local" value={start} onChange={e=>setStart(e.target.value)}/><select className="app-input" value={kind} onChange={e=>setKind(e.target.value)}><option>work</option><option>meeting</option><option>followup</option><option>deadline</option><option>campaign</option><option>reminder</option></select><div/><button className="primary-button" disabled={busy} onClick={add}>Add</button></div>}
