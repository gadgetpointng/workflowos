import WorkspaceShell from '@/components/WorkspaceShell';
export default function MarketplaceJobsPage() {
  return <main className="mx-auto max-w-7xl px-6 py-10"><div className="text-sm font-semibold uppercase tracking-[.2em] text-slate-500">Channel operations</div><h1 className="mt-2 text-3xl font-bold">Marketplace Jobs</h1><p className="mt-2 max-w-3xl text-slate-600">A connector-neutral queue for listing creation, price updates, inventory sync, order import and performance checks across supported marketplaces.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{[['Listing work','Create/update authorized product listings'],['Demand review','Turn marketplace demand signals into staff work'],['Sync jobs','Inventory, orders and performance sync']].map(([t,b])=><div key={t} className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="font-semibold">{t}</h2><p className="mt-2 text-sm text-slate-600">{b}</p></div>)}</div><div className="mt-6 rounded-3xl border border-dashed bg-white p-6 text-sm text-slate-600">Jobs are queued through <code>/api/marketplace-jobs</code>. A marketplace-specific worker can execute them only when an approved connector is configured.</div></main>;
}